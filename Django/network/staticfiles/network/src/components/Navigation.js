import React from 'react';
import { Link } from 'react-router-dom';

const Navigation = ({ user, isAuthenticated }) => {
  // We're using Django's navigation, so this component is optional
  // It can be used if we want to fully migrate to React routing in the future
  return null;
};

export default Navigation; 